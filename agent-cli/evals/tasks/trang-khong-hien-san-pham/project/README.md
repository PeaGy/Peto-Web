# shop-page

Trang giới thiệu sản phẩm của cửa hàng, đọc danh sách từ `products.json`.

## Chạy thử

Trang tải `products.json` bằng `fetch`, nên phải mở qua một web server, không mở thẳng file:

    python -m http.server 8765

rồi vào http://localhost:8765.
